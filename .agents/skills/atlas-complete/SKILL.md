---
name: atlas-complete
description: Complete the current milestone and prepare next work by following the canonical complete command doc.
---

Read the canonical complete command markdown at `.claude/commands/atlas/complete.md` and follow it exactly.

Codex command style:
- Replace any `/atlas:<command>` references from canonical docs with `$atlas:<command>` in Codex responses.

Safety: verify all phases are complete before closing milestone artifacts.
