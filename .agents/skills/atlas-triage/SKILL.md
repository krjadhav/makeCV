---
name: atlas-triage
description: Pull and triage issues from external sources for planning by following the canonical triage command doc.
---

Read the canonical triage command markdown at `.claude/commands/atlas/triage.md` and follow it exactly.

Codex command style:
- Replace any `/atlas:<command>` references from canonical docs with `$atlas:<command>` in Codex responses.

Safety: keep source data traceable and avoid changing scope without explicit confirmation.
