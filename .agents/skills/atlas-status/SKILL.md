---
name: atlas-status
description: Check project progress and suggest the next action by following the canonical status command doc.
---

Read the canonical status command markdown at `.claude/commands/atlas/status.md` and follow it exactly.

Codex command style:
- Replace any `/atlas:<command>` references from canonical docs with `$atlas:<command>` in Codex responses.

Safety: read and report state only; do not modify files.
